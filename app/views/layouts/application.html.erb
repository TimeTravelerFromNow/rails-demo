<!DOCTYPE html>
<html>
  <head>
    <title>Sebastian's Blog Public</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class='bg-light visitors'>

    <nav class="navbar navbar-customclass navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">

      <%= link_to "Sebastian's Blog",  root_path, class:"navbar-brand" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-md-auto mb-2 mb-lg-0">
          <%= link_to "Home", root_path, class: 'nav-link active' %>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-md-auto mb-2 mb-lg-0">
          <%= link_to "sign up", new_user_registration_path, method: :delete, class: 'nav-link active'%>
          <% if user_signed_in? %>
            <%= button_to "logout", destroy_user_session_path, method: :delete, class: 'nav-link active'%>
          <% else %>
            <%= link_to "sign in", new_user_session_path, method: :delete, class: 'nav-link active'%>
          <% end %>
        </div>

 </nav>
 <p class="notice"><%= notice %></p>
 <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
